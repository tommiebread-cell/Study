{
  "name": "gmm-vault",
  "version": "1.0.0",
  "private": true,
  "description": "An interactive vault for dynamic panel estimation, with the GMM estimator implemented three times over and checked against itself.",
  "type": "module",
  "scripts": {
    "start": "node tools/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "snippets": "node tools/snippets.mjs",
    "check:snippets": "node tools/snippets.mjs --check",
    "crosscheck": "node tools/crosscheck.mjs",
    "sim": "node reference/js/cli.mjs",
    "build:cpp": "make -C reference/cpp",
    "build:csharp": "dotnet build reference/csharp/DynamicPanel",
    "verify": "npm run check:snippets && npm test && npm run crosscheck"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
